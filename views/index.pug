extends layout

block content
        h1 #{title}
        ul.list-group.mt-4
            each customer, i in customers
                li.list-group-item 
                    a(href="/customer/"+customer._id) #{customer.firstName} #{customer.lastName} : #{customer.email}
